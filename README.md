# sups

## Calculator

This repository contains a simple command line calculator written in Python.

### Usage

Run the script with two numbers and an operator:

```bash
python3 calculator.py 5 + 3
```

Supported operators are `+`, `-`, `*`, and `/`.

